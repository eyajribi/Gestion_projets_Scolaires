<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".MainActivity"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ScolabStudentApp"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <!-- Login est l'activité de lancement -->
        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.ScolabStudentApp.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Activité d'inscription -->
        <activity
            android:name=".RegisterActivity"
            android:exported="false"
            android:theme="@style/Theme.ScolabStudentApp.NoActionBar" />

        <!-- Activité principale avec la navigation -->
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:theme="@style/Theme.ScolabStudentApp" />

        <!-- Les activités de détail restent indépendantes -->
        <activity
            android:name=".activities.ProjectDetailActivity"
            android:exported="false"
            android:theme="@style/Theme.ScolabStudentApp.NoActionBar" />

        <activity
            android:name=".DeliverablesActivity"
            android:exported="false"
            android:theme="@style/Theme.ScolabStudentApp.NoActionBar" />

        <!-- Authentification GitHub (si conservée) -->
        <activity
            android:name=".GitHubAuthActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:host="github-auth"
                    android:scheme="scolabstudentapp" />
            </intent-filter>
        </activity>

    </application>

</manifest>
